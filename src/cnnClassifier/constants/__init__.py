# we mention here : 
# 1. all config comes from config --> config.yaml file and 
# 2. all parameter comes from params.yaml file 
from pathlib import Path 

CONFIG_FILE_PATH = Path("config/config.yaml") 
PARAMS_FILE_PATH = Path("params.yaml") 